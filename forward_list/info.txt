This text file shows what methods does my forward linked list container provide

clear()-clears data inside a list
delete_at(const std::size_t k)-deletes a certain node
get_size()-returns the size of the list
insert_back(T value)-inserts a value at the end of the list
operator[const std::size_t k]-returns the value at index k
insert_at(const iterator arg, T value)
move constructor
copy constructor